<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Gestión Llaves</title>
    <!-- Bootstrap 5 CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <!-- Custom CSS -->
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>
<body>
    <!-- Sidebar -->
    <div class="sidebar-wrapper">
        <div class="sidebar-brand">
            <div class="sidebar-brand-icon"><i class="bi bi-key-fill"></i></div>
            UPDS Llaves
        </div>

        <div class="sidebar-menu">
            <div class="menu-label">Menú Principal</div>
            <a href="/" class="menu-item @(ViewContext.RouteData.Values["Controller"]?.ToString() == "Home" ? "active" : "")">
                <i class="bi bi-grid-1x2"></i> Dashboard
            </a>
            <a href="#" class="menu-item">
                <i class="bi bi-arrow-left-right"></i> Préstamos
            </a>
            <a href="#" class="menu-item">
                <i class="bi bi-calendar-check"></i> Reservas
            </a>

            <div class="menu-label">Catálogos</div>
            <a href="#" class="menu-item">
                <i class="bi bi-key"></i> Llaves
            </a>
            <a href="#" class="menu-item">
                <i class="bi bi-door-open"></i> Ambientes
            </a>
            <a href="#" class="menu-item">
                <i class="bi bi-people"></i> Personas
            </a>

            <div class="menu-label">Sistema</div>
            <a href="#" class="menu-item">
                <i class="bi bi-file-earmark-bar-graph"></i> Reportes
            </a>
            <a href="#" class="menu-item">
                <i class="bi bi-shield-lock"></i> Usuarios y Roles
            </a>
        </div>

        <div class="sidebar-footer">
            <a href="#" class="menu-item text-danger">
                <i class="bi bi-box-arrow-right"></i> Cerrar Sesión
            </a>
        </div>
    </div>

    <!-- Main Content -->
    <div class="main-wrapper">
        <!-- Top Navbar -->
        <header class="top-navbar">
            <div class="page-title">
                <h1>Bienvenido al Sistema</h1>
                <p>Hola, Usuario. Aquí tienes el resumen de hoy.</p>
            </div>
            <div class="top-actions">
                <button class="action-btn" title="Modo Oscuro">
                    <i class="bi bi-moon"></i>
                </button>
                <button class="action-btn" title="Notificaciones">
                    <i class="bi bi-bell"></i>
                </button>
                <img src="https://ui-avatars.com/api/?name=Admin+User&background=0369a1&color=fff" alt="Usuario" class="user-avatar" />
            </div>
        </header>

        <!-- Dynamic Content -->
        <main role="main" class="content-area">
            @RenderBody()
        </main>
    </div>

    <!-- Bootstrap JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
