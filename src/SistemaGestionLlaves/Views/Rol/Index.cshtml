@model IEnumerable<SistemaGestionLlaves.Models.Rol>
@{
    ViewData["Title"] = "Roles";
}
<h2>Roles</h2>
<p><a asp-action="Create">Nuevo Rol</a></p>
<table>
    <thead>
        <tr>
            <th>ID</th>
            <th>Nombre</th>
            <th>Descripción</th>
            <th>Estado</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    @foreach(var item in Model)
    {
        <tr>
            <td>@item.IdRol</td>
            <td>@item.NombreRol</td>
            <td>@item.Descripcion</td>
            <td>@item.Estado</td>
            <td>
                <a asp-action="Details" asp-route-id="@item.IdRol">Ver</a> |
                <a asp-action="Edit" asp-route-id="@item.IdRol">Editar</a> |
                <a asp-action="Delete" asp-route-id="@item.IdRol">Eliminar</a>
            </td>
        </tr>
    }
    </tbody>
</table>